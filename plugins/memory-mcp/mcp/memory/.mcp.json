{
  "mcpServers": {
    "memory": {
      "command": "docker",
      "args": [
        "mcp",
        "gateway",
        "run",
        "--catalog",
        "${CLAUDE_PLUGIN_ROOT}/mcp/memory/memory-catalog.yaml"
      ]
    }
  }
}
